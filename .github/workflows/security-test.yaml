name: DevSecOps Scan (Dual Engine)

on: [push, pull_request]

jobs:
  security-audit:
    runs-on: ubuntu-latest
    name: Security Audit
    permissions:
      contents: read
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # -----------------------------------------------------------
      # 1. SECRET SCAN (Gitleaks)
      # -----------------------------------------------------------
      - name: Run Gitleaks
        run: |
          curl -sS -L https://github.com/gitleaks/gitleaks/releases/download/v8.18.2/gitleaks_8.18.2_linux_x64.tar.gz -o gitleaks.tar.gz
          tar -xf gitleaks.tar.gz gitleaks
          chmod +x gitleaks
          ./gitleaks detect -v --source . --report-path gitleaks-report.json --report-format json || true

      # -----------------------------------------------------------
      # 2. SAST PART A: BANDIT (The Heavy Hitter)
      # -----------------------------------------------------------
      - name: Run Bandit (Python Security)
        run: |
          pip install bandit
          # -r: recursive, -f json: format, -o: output
          # || true prevents pipeline failure
          bandit -r . -f json -o bandit-report.json || true

      # -----------------------------------------------------------
      # 3. SAST PART B: SEMGREP (Modern Rules)
      # -----------------------------------------------------------
      - name: Run Semgrep
        run: |
          pip install semgrep
          # We revert to 'p/default' + 'p/security-audit' for broad coverage
          semgrep scan --config=p/default --config=p/security-audit --json -o semgrep-report.json || true

      # -----------------------------------------------------------
      # 4. SCA SCAN (Trivy)
      # -----------------------------------------------------------
      - name: Install Trivy
        run: |
          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sh -s -- -b /usr/local/bin
      
      - name: Trivy Scan
        run: |
          trivy fs . --format json --output trivy-report.json --exit-code 0

      # -----------------------------------------------------------
      # 5. GENERATE UNIFIED DASHBOARD
      # -----------------------------------------------------------
      - name: Generate Dashboard
        if: always()
        shell: python
        run: |
          import json
          import os

          summary_content = "# 🛡️ Security Scan Report\n\n"
          
          # --- LOAD DATA ---
          gitleaks_data = []
          semgrep_data = []
          bandit_data = []
          trivy_data = []

          # 1. Gitleaks
          if os.path.exists("gitleaks-report.json"):
              try:
                  with open("gitleaks-report.json") as f:
                      gitleaks_data = json.load(f)
              except: pass

          # 2. Semgrep
          if os.path.exists("semgrep-report.json"):
              try:
                  with open("semgrep-report.json") as f:
                      semgrep_data = json.load(f).get('results', [])
              except: pass

          # 3. Bandit (New!)
          if os.path.exists("bandit-report.json"):
              try:
                  with open("bandit-report.json") as f:
                      data = json.load(f)
                      bandit_data = data.get('results', [])
              except: pass

          # 4. Trivy
          if os.path.exists("trivy-report.json"):
              try:
                  with open("trivy-report.json") as f:
                      data = json.load(f)
                      for result in data.get('Results', []):
                          for v in result.get('Vulnerabilities', []):
                              trivy_data.append({
                                  'pkg': v.get('PkgName', 'unknown'),
                                  'title': v.get('Title', 'No description available'),
                                  'severity': v.get('Severity', 'UNKNOWN'),
                                  'id': v.get('VulnerabilityID', 'unknown')
                              })
              except: pass

          # --- COMBINE SAST ---
          total_sast_count = len(semgrep_data) + len(bandit_data)
          
          # Icons
          gl_icon = "🔴" if len(gitleaks_data) > 0 else "✅"
          sast_icon = "🔴" if total_sast_count > 0 else "✅"
          tr_icon = "🟠" if len(trivy_data) > 0 else "✅"

          summary_content += f"""
          | Category | Status | Issues |
          | :--- | :--- | :---: |
          | **Secrets** | {gl_icon} | {len(gitleaks_data)} |
          | **Code (SAST)** | {sast_icon} | {total_sast_count} |
          | **Deps (SCA)** | {tr_icon} | {len(trivy_data)} |
          
          ---
          """

          # --- SECTIONS ---

          # GITLEAKS
          if gitleaks_data:
              summary_content += f"""
          <details><summary><strong>🔻 Secrets Details ({len(gitleaks_data)})</strong></summary>
          
          | Severity | File | Line | Variable |
          | :--- | :--- | :--- | :--- |
          """
              for s in gitleaks_data:
                  raw_match = s.get('Match', '')
                  safe_match = (raw_match[:20] + '...') if len(raw_match) > 20 else raw_match
                  safe_match = safe_match.replace('|', '\|')
                  summary_content += f"| **🔴 CRITICAL** | `{s.get('File', '?')}` | {s.get('StartLine', '?')} | `{safe_match}` |\n"
              summary_content += "</details>\n\n"

          # SAST (Combined Bandit + Semgrep)
          if total_sast_count > 0:
              summary_content += f"""
          <details><summary><strong>🔻 Code Issues ({total_sast_count})</strong></summary>
          
          | Tool | Severity | File | Line | Issue |
          | :--- | :--- | :--- | :--- | :--- |
          """
              # Add Bandit Rows
              for b in bandit_data:
                  sev = b.get('issue_severity', 'MEDIUM')
                  icon = "🔴" if sev == "HIGH" else "🟠"
                  summary_content += f"| Bandit | **{icon} {sev}** | `{b.get('filename', '?')}` | {b.get('line_number', '?')} | {b.get('issue_text', '?')} |\n"

              # Add Semgrep Rows
              for s in semgrep_data:
                  sev = s.get('extra', {}).get('severity', 'UNKNOWN')
                  icon = "🔴" if sev == "ERROR" else "hk"
                  msg = s.get('extra', {}).get('message', '').replace('\n', ' ')
                  summary_content += f"| Semgrep | **{icon} {sev}** | `{s.get('path', '?')}` | {s.get('start', {}).get('line', '?')} | {msg} |\n"
              
              summary_content += "</details>\n\n"

          # TRIVY
          if trivy_data:
              summary_content += f"""
          <details><summary><strong>🔻 Vulnerable Dependencies ({len(trivy_data)})</strong></summary>
          
          | Severity | Package | Vuln Name | ID |
          | :--- | :--- | :--- | :--- |
          """
              for t in trivy_data:
                  icon = "🔴" if t['severity'] in ["CRITICAL", "HIGH"] else "hk"
                  title = t['title'].replace('|', '-')
                  summary_content += f"| **{icon} {t['severity']}** | `{t['pkg']}` | {title} | {t['id']} |\n"
              summary_content += "</details>\n"

          with open(os.environ['GITHUB_STEP_SUMMARY'], 'a') as f:
              f.write(summary_content)
